import { describe, it, expect, test } from 'vitest';

// NOTE: Component not implemented yet (T026). These are TDD placeholders to be filled when UI exists.

describe('[US1] ProductList page', () => {
  test.todo('renders a list of products fetched from /api/products');
  test.todo('formats price with $ and two decimals');
  test.todo('shows empty state when no products');
});
